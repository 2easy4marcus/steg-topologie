# Safe local-only defaults. Replace these values outside local development.
CRON_SECRET=local-cron-secret
OPS_SECRET=local-ops-secret

# Optional hosted Turso configuration. Leave empty to use local libSQL.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
